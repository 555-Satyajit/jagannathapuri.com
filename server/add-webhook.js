// This script will create a Razorpay Webhook endpoint to catch payments
const fs = require('fs');
const path = require('path');

const routesPath = path.join(__dirname, 'src', 'routes', 'api.js');

// To be done later if requested
