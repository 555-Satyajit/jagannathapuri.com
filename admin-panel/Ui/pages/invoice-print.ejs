<!DOCTYPE html>
<html lang="en" class="light-style layout-wide " dir="ltr" data-theme="theme-default" data-assets-path="/admin-assets/"
    data-template="vertical-menu-template">

<head>
    <meta charset="utf-8" />
    <meta name="viewport"
        content="width=device-width, initial-scale=1.0, user-scalable=no, minimum-scale=1.0, maximum-scale=1.0" />
    <base href="<%= baseUrl || process.env.BASE_URL || 'http://localhost:3000' %>/" />

    <title>Invoice #<%= order.orderNumber %> - Jagannathapuri</title>

    <meta name="description" content="Start your development with a Dashboard for Bootstrap 5" />
    <meta name="keywords" content="dashboard, bootstrap 5 dashboard, bootstrap 5 design, bootstrap 5">

    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/admin-assets/img/favicon/favicon.ico" />

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&family=Rubik:ital,wght@0,300;0,400;0,500;0,600;0,700;1,300;1,400;1,500;1,600;1,700&display=swap"
        rel="stylesheet">

    <!-- Icons -->
    <link rel="stylesheet" href="/admin-assets/vendor/fonts/boxicons.css" />
    <link rel="stylesheet" href="/admin-assets/vendor/fonts/fontawesome.css" />
    <link rel="stylesheet" href="/admin-assets/vendor/fonts/flag-icons.css" />

    <!-- Core CSS -->
    <link rel="stylesheet" href="/admin-assets/vendor/css/rtl/core.css" class="template-customizer-core-css" />
    <link rel="stylesheet" href="/admin-assets/vendor/css/rtl/theme-default.css"
        class="template-customizer-theme-css" />
    <link rel="stylesheet" href="/admin-assets/css/demo.css" />

    <!-- Page CSS -->
    <link rel="stylesheet" href="/admin-assets/vendor/css/pages/app-invoice-print.css" />

    <style>
        /* Specific overrides for printing/PDF generation */
        @media print {
            .invoice-print {
                padding: 0 !important;
                margin: 0 !important;
            }

            body {
                background-color: white !important;
            }
        }
    </style>
</head>

<body>

    <div class="invoice-print p-5">

        <div class="d-flex justify-content-between flex-row">
            <div class="mb-4">
                <div class="d-flex align-items-center gap-2 mb-3">
                    <img src="/admin-assets/img/logo/jp-icon-logo.png" alt="Jagannathapuri" style="max-height: 70px; width: auto;">
                    <span class="app-brand-text h3 mb-0 fw-bold" style="line-height: 1;">Jagannathapuri</span>
                </div>
                <p class="mb-1">123, Puri Temple Road, Odisha, India</p>
                <p class="mb-1">Phone: +91 98765 43210</p>
                <p class="mb-0">Email: support@jagannathapuri.com</p>
            </div>
            <div>
                <h4 class="fw-medium">INVOICE #<%= order.orderNumber %>
                </h4>
                <div class="mb-2">
                    <span class="text-muted">Date Issued:</span>
                    <span class="fw-medium">
                        <%= new Date(order.date).toLocaleDateString() %>
                    </span>
                </div>
                <!--
      <div>
        <span class="text-muted">Date Due:</span>
        <span class="fw-medium">May 25, 2021</span>
      </div>
      -->
            </div>
        </div>

        <hr />

        <div class="row d-flex justify-content-between mb-4">
            <div class="col-sm-6 w-50">
                <h6>Invoice To:</h6>
                <p class="mb-1">
                    <%= order.customer.fullName %>
                </p>
                <% if (order.shippingAddress) { %>
                    <p class="mb-1">
                        <%= order.shippingAddress.addressLine1 %>
                    </p>
                    <p class="mb-1">
                        <%= order.shippingAddress.city %>, <%= order.shippingAddress.state %>
                                <%= order.shippingAddress.zipCode %>
                    </p>
                    <p class="mb-1">
                        <%= order.shippingAddress.country %>
                    </p>
                    <% } %>
                        <p class="mb-1">
                            <%= order.customer.phone || 'N/A' %>
                        </p>
                        <p class="mb-0">
                            <%= order.customer.email %>
                        </p>
            </div>
            <div class="col-sm-6 w-50">
                <h6>Bill To:</h6>
                <table>
                    <tbody>
                        <tr>
                            <td class="pe-3">Total Due:</td>
                            <td class="fw-medium">₹<%= order.totalAmount.toLocaleString() %>
                            </td>
                        </tr>
                        <tr>
                            <td class="pe-3">Payment Method:</td>
                            <td>
                                <%= (order.paymentMethod || 'N/A' ).split('_')[0].toUpperCase() %>
                            </td>
                        </tr>
                        <tr>
                            <td class="pe-3">Bank name:</td>
                            <td>State Bank of India</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>

        <div class="table-responsive">
            <table class="table border-top m-0">
                <thead>
                    <tr>
                        <th>Item</th>
                        <!-- <th>Description</th> -->
                        <th>Price</th>
                        <th>Qty</th>
                        <th>Total</th>
                    </tr>
                </thead>
                <tbody>
                    <% order.items.forEach(function(item) { %>
                        <tr>
                            <td>
                                <%= item.product ? item.product.product_name : 'Unknown Item' %>
                            </td>
                            <!-- <td>HTML Admin Template</td> -->
                            <td>₹<%= item.price.toFixed(2) %>
                            </td>
                            <td>
                                <%= item.quantity %>
                            </td>
                            <td>₹<%= (item.price * item.quantity).toFixed(2) %>
                            </td>
                        </tr>
                        <% }); %>

                            <tr>
                                <td colspan="2" class="align-top px-4 py-3">
                                    <p class="mb-2">
                                        <span class="me-1 fw-medium">Salesperson:</span>
                                        <span>Jagannathapuri</span>
                                    </p>
                                    <span>Thanks for your business</span>
                                </td>
                                <td class="text-end px-4 py-3">
                                    <p class="mb-2">Subtotal:</p>
                                    <!-- <p class="mb-2">Discount:</p> -->
                                    <p class="mb-2">Tax:</p>
                                    <p class="mb-2">Shipping:</p>
                                    <p class="mb-0">Total:</p>
                                </td>
                                <td class="px-4 py-3">
                                    <p class="fw-medium mb-2">₹<%= Number(order.subtotal || order.totalAmount ||
                                            0).toFixed(2) %>
                                    </p>
                                    <!-- <p class="fw-medium mb-2">$00.00</p> -->
                                    <p class="fw-medium mb-2">₹0.00</p>
                                    <p class="fw-medium mb-2">₹<%= Number(order.shippingFee || 0).toFixed(2) %>
                                    </p>
                                    <p class="fw-medium mb-0">₹<%= Number(order.totalAmount || 0).toFixed(2) %>
                                    </p>
                                </td>
                            </tr>
                </tbody>
            </table>
        </div>

        <div class="row">
            <div class="col-12">
                <span class="fw-medium">Note:</span>
                <span>Thank you for shopping with us!</span>
            </div>
        </div>
    </div>

    <script src="/admin-assets/js/main.js"></script>

    <!-- Auto Print Script (Only for browser view, not PDF generation) -->
    <script>
        // window.print();
    </script>

</body>

</html>